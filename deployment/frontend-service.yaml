apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: frontend
spec:
  ports:
    - port: 8080
      name: "8100"
      targetPort: 8080
      protocol: TCP
  selector:
    app: frontend
