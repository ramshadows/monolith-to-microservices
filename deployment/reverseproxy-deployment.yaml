apiVersion: apps/v1
kind: Deployment
metadata:
  name: reverseproxy
  labels:
    service: reverseproxy
spec:
  replicas: 2
  selector:
    matchLabels:
      service: reverseproxy
  template:
    metadata:
      labels:
        service: reverseproxy
    spec:
      containers:
        - image: docker.io/ramshadows/reverseproxy:v7
          name: reverseproxy
          imagePullPolicy: Always
          resources:
            requests:
              memory: "80Mi"
              cpu: "80m"
            limits:
              memory: "1024Mi"
              cpu: "80m"
          ports:
            - containerPort: 8080
      restartPolicy: Always
